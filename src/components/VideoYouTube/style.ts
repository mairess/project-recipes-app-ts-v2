import styled from 'styled-components';

export const Title = styled.h1`
    color: #1A1B1C;
    font-size: 1.25rem;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
    margin: 1rem 0 0.6rem 1rem;
`;
